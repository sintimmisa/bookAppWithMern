{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is the backend of a simple book app project with  MERN Stack ",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"npm run dev\" \" cd ./client && npm start",
    "dev": "nodemon  server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sintim-Misah Duah G.",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "concurrently": "^5.3.0",
    "config": "^3.3.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.10.0",
    "validation": "0.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}